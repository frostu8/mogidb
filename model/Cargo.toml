[package]
name = "mogidb-model"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { workspace = true }
serde_repr = { workspace = true }
derive_more = { workspace = true, features = ["display", "error", "deref", "from"] }
